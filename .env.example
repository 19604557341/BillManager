# 环境变量模板（此文件会提交到 git，仅放占位说明，不要填真实密钥）
#
# 用法：复制为项目根目录下的 .env 并填入真实值，.env 已被 .gitignore 排除。
#   copy .env.example .env
#
# 生产 / CI 环境无需 .env，直接设置同名系统环境变量即可，优先级更高。

# ===========================================================================
# 关于「密码在哪里解密」—— 不需要解密，本项目没有任何加解密逻辑
# ===========================================================================
# .env 中的 DB_PASSWORD 存放的就是【明文密码本身】，不是密文。
# 因此不存在解密步骤，项目里也没有（且不需要有）任何解密文件。
# 全仓库检索 encrypt / decrypt / cipher / jasypt / AES / Base64 / vault 均无匹配。
#
# 密码从文件到登录成功的完整链路：
#   1) 启动时 Spring Boot 通过 application.yml 里的
#      spring.config.import: "optional:file:.env[.properties]"
#      读取 .env（按 properties 格式、UTF-8 解析），把 DB_PASSWORD 放入 Environment；
#   2) application.yml 中的 ${DB_PASSWORD} 占位符被原样替换成该明文值，
#      连同 ${DB_USERNAME:billmanager} 一起交给 HikariCP 构建数据源；
#   3) MySQL JDBC 驱动在认证握手阶段提交这个密码。
#
# 服务端为什么不存明文、也不需要解密：
#   mysql.user 表中该账户的 plugin 为 caching_sha2_password，存的是密码的
#   【单向哈希】而非密文。服务端拿客户端提交的密码做同样哈希后比对是否一致。
#   哈希不可逆，不存在对应的解密过程——这是「校验」，不是「解密」。
#
# 那么明文放在 .env 里靠什么保护？
#   - .env 已被 .gitignore 排除，不进入版本库（这正是本次改造的目的：
#     此前密码硬编码在 application.yml 中，会随仓库一起分发）；
#   - 依赖操作系统的文件权限限制可读取范围；
#   - 生产 / CI 环境不落盘，直接由环境变量或密钥管理服务注入，
#     且系统环境变量优先级高于 .env。
#   这就是通行的「配置与密钥分离」做法，安全性来自「不泄露」而非「加密存储」。
#
# 若确实需要对配置文件做静态加密，可另行引入 Jasypt（jasypt-spring-boot）
# 或 HashiCorp Vault，但两者都需要额外管理一把主密钥，本项目目前未采用。
# ===========================================================================

# 数据库账户名（可选，默认 billmanager）
# 建议使用 bill_manager 库的专用受限账户，不要用 root 直连。
DB_USERNAME=billmanager

# 数据库密码（必填，明文存放；未设置时应用启动会因占位符无法解析而快速失败）
DB_PASSWORD=

# ---------------------------------------------------------------------------
# 专用账户的创建方式（用 root 登录 MySQL 后执行，把 '你的密码' 换成强随机密码）
#
#   CREATE USER 'billmanager'@'localhost' IDENTIFIED BY '你的密码';
#   GRANT SELECT, INSERT, UPDATE, DELETE ON bill_manager.* TO 'billmanager'@'localhost';
#
# 只授予 DML 权限即可满足应用运行，无需 DDL 与管理权限。
#
# 密码可用 PowerShell 生成 32 位随机串。注意「加密安全随机」指的是
# 【随机数来源】是密码学安全的 RNG，生成结果仍是明文密码，不需要解密：
#
#   $c='ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_'
#   $b=New-Object byte[] 32
#   [System.Security.Cryptography.RandomNumberGenerator]::Create().GetBytes($b)
#   -join ($b | ForEach-Object { $c[$_ -band 63] })
#
# 字符集刻意取 64 个字符，配合 -band 63 可做无偏差取模，且避开
# properties 转义符 \ 与 SQL 引号 ' ，也不含 cmd/PowerShell 的特殊字符。
# ---------------------------------------------------------------------------
